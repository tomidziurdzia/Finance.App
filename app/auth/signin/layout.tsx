const title = "Sign in for Finance";
const description = "Track and Manage Expenses.";

export const metadata = {
  title,
  description,
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
